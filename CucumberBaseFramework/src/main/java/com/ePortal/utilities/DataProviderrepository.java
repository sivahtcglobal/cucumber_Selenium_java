package com.ePortal.utilities;

import java.lang.reflect.Method;

import org.testng.annotations.DataProvider;

public class DataProviderrepository {

	/*
	 * @DataProvider(name = "IUTestData") public Object[][]
	 * dataProviderMethod(Method method) throws MyOwnException {
	 * 
	 * 
	 * Object[][] testDataArray = null; try { testDataArray =
	 * Excel.getData(System.getProperty("user.dir") +
	 * "\\src\\test\\resources\\TestData\\" + prop.getProperty("TestDataFileName"),
	 * prop.getProperty("InitAppSheetName"), testCaseId); } catch (Exception exp) {
	 * log.error(exp.getMessage());
	 * throwException("UNABLE TO EXECUTE DATA PROVIDER SECTION FROM THE METHOD dataProviderMethod\n"
	 * + exp.getMessage() + "\n"); }
	 * log.info("SUCCESSFULLY READ THE TEST DATA FOR (" + method +
	 * ") SECURITY TYPE USING DATAPROVIDER FROM EXCEL WORKBOOK"); return
	 * testDataArray; }
	 */

}
